    # cursor.execute("Select company_name from company")
    # company_name=cursor.fetchall()
    # for i in company_name:
    #     for j in i:
    #         q=f"alter table stock_price add column {j} varchar(100)"
    #         cursor.execute(q)
    # con.commit()
    